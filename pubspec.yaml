name: pulsar_cli
description: A sample command-line application with basic argument parsing.
version: 0.0.1
repository: https://github.com/IgnacioFernandez1311/pulsar_cli

topics:
  - CLI
  - web
  - frontend


environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  path: ^1.9.1
  webdev: ^3.0.8

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
